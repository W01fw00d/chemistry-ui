import React from 'react';

import { storiesOf } from '@storybook/react';

import Footer from '../footer';

storiesOf('2. Molecules|Layout.Footer', module).add('default', () => <Footer>Footer Bar</Footer>);
