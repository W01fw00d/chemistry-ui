import React from 'react';

import Centered from '../atoms/layout/centered';
import Spinner from '../atoms/spinner';

export default () => {
  return (
    <Centered>
      <Spinner />
    </Centered>
  );
};
